#include <stdio.h>
#include <math.h>

int main(){

int i, j, k, l, f;

    printf("Enter a number: ");
    scanf("%d", &i);
    printf("\nEnter a Float number: ");
    scanf("%f", &j);
    printf("\nEnter two Integers: ");
    scanf("%d", &k);
    scanf("%d", &l);
    printf("\nSQUARE ROOT is: %d", sqrt(i));
    printf("\Round Number - Celing is: %f", ceil(f));
    printf("\Round Number - Floor is: %f", floor(f));
    printf("\Power is: %d", pow(k, l));

return 0;
}
